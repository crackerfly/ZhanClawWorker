# 被控端 Worker 合约复核

日期：2026-08-14

## 范围与证据等级

本复核只讨论 Windows 被控端及随包 `p2p-agent.exe`。用于查漏补缺的原始材料包括 Worker 架构说明、配置示例和测试用例；面向其他产品的集成材料及其他可执行文件不属于本项目范围。

原始 Worker 文档以 `0.1.0-integration.3` 为基线，而本仓库冻结的是 `0.1.0-integration.4`：

- 文件：`runtime/p2p-agent.exe`
- SHA-256：`a2b36af5f2623ddd2f91d223f471abe9d8d957fb2dca6a566e02b2dbd04dd5e9`
- PE：AMD64 / Console
- 签名者：`StarSoftComm(China) Ltd.`，并固定叶证书与 SPKI 指纹

因此 `.3` 文档只能证明原设计意图并提供测试线索，不能证明 `.4` 二进制内部语义。对 `.4` 的直接证据分为三类：精确字节哈希与签名、当前管理器源码可证明的行为、Windows 真机上对冻结二进制的协议与安全测试。没有 `.4` Go 源码时，不能把旧文档描述升级成已证明事实。

## 当前配置契约

当前管理器创建和验证的默认配置如下：

| 项 | 当前行为 | 证据边界 |
|---|---|---|
| `agent_tags` | 向导与默认配置均为 `worker` | 可由当前 C# 源码证明 |
| `bootstrap_addrs` | 两条发行包预置的公网 TCP/WS multiaddr | 可由当前 C# 源码证明 |
| `rendezvous_group` | `p2p-agents` | 可由当前 C# 源码证明 |
| `api_listen` | `127.0.0.1:7432` | 管理器强制精确匹配并使用 Token 鉴权 |
| `allowed_peers` | 初始为空，只接受规范的完整 base58btc libp2p PeerID | 可证明输入校验和配置写入；载荷最终执行语义仍需 `.4` 真机验收 |
| `command_journal_file` | `C:/ProgramData/P2PAgent/agent-command-journal.jsonl` | 管理器强制固定路径 |
| 资源默认值 | 并行任务数 4，单次传输上限 8 GiB | 可证明配置写入；载荷如何执行仍需 `.4` 验收 |

向导当前不提供 Bootstrap 编辑入口。首次安装会写入上述公网地址并在启动后尝试服务器发现、Router 与 Relay，用户不能在安装前选择仅局域网模式。仅局域网部署必须先完成安装，再到“设置”清空 Bootstrap 地址并重启 Agent；这项联网行为应在部署计划和真机网络捕获中明确验证。

## 回环 API 与授权状态

当前界面只把 `/v1/info` 的本机 PeerID/版本，以及 `/v1/peers` 中的远端 PeerID/path 作为 `integration.4` 的稳定可读字段。mDNS、Relay、Router、监听地址和任务容量没有被当前界面当作可靠的 `.4` 状态证据；相关路径需结合日志和真机网络测试诊断。

保存 `allowed_peers` 后，管理器会停止并启动计划任务，等待新进程的鉴权回环 API 健康，然后把当时磁盘上的列表保存为“健康重启配置快照”。这能证明重启和回环 API 成功，却不能证明载荷内部白名单已经通过 API 回读，也不能证明某类远端请求的最终接受结果。界面和文档不得把该快照称为运行时策略证明。

## 必须披露的运行风险

原始 Worker 合约没有定义以下防护，当前管理器也无法在载荷内部补上：

- 文件操作的允许根目录或路径约束；
- URL 下载的出站主机白名单、地址解析限制或 SSRF 防护；
- Server Router 对转发帧元数据与 payload 的不可见性；现有文档未声明额外应用层端到端加密。

因此应只配置可信远端来源，使用受限专用 Windows 账户运行 Agent，并通过防火墙、代理或主机网络策略限制出站访问。发布验收必须包含路径越界、URL 回环/内网地址、DNS 重绑定和 Router 可见性测试；在这些测试完成前不能声称载荷具有相应防护。

## journal、日志与诊断

- 可用 Worker 合约把 command journal 用于 ACK、去重和崩溃恢复，并采用追加写入；没有自动压缩流程。管理器会读取和导出，但不会压缩它。
- journal 接近 512 MiB 时必须提前安排保持 ACK/去重连续性的安全归档。超过 512 MiB 会阻断安装、修复或卸载中需要一致停机快照的维护；直接清空或用旧副本覆盖较新 journal 都不安全。
- `AgentHost` 只在下一次启动载荷之前检查日志，并在当时 `agent.log` 大于 8 MiB 时移动为 `agent.log.1`。常驻进程的一次持续运行期间日志可以继续超过 8 MiB，8 MiB 不是实时硬上限。
- 默认诊断会读取 API Token 以鉴权查询 `127.0.0.1`，但不会输出、显示或保存 Token 值。私网密钥和设备私钥也只报告文件元数据；默认模式会省略原始 journal、Agent 日志、bootstrap 地址和业务输出。

## 安装快照与密钥生命周期

安装、修复和可回滚卸载会先确认 Agent 停止，再捕获受保护的一致快照。操作成功并完成清理核验时删除快照；失败、回滚结果不确定或删除失败时保留恢复材料并报告路径。若停机后产生了更新 journal，自动恢复保留较新状态，绝不把旧 journal 快照直接覆盖回去。

有效的既有 `swarm.key` 会在升级、修复以及保留数据后的再次安装中继续保留。替换仓库中的 `runtime/swarm.key` 或重新运行安装器只影响新部署，不会自动轮换已部署 key。当前产品没有自动或安全原位轮换入口；轮换必须在统一维护窗口停止相关 Agent，由受审查的管理员流程替换 key、恢复精确 ACL、复核 owner/DACL/格式/哈希，再统一启动并验证连接。

## `.4` 发布前必做验收

- 标准用户、同账户 UAC、凭据式 UAC下的首装、升级、修复、回滚和卸载。
- 首装公网连接与清空 Bootstrap 后的仅局域网行为。
- `allowed_peers` 为空、单一合法 PeerID、未授权 PeerID、损坏配置与健康重启失败。
- 路径越界、URL 到回环/链路本地/内网地址、DNS 重绑定与重定向。
- Router 路径下的元数据和 payload 可见性，以及是否存在任何另行声明的应用层加密。
- journal 持续增长、接近和超过 512 MiB、维护中出现较新 journal，以及失败后恢复材料保留。
- 单次长时间运行让日志超过 8 MiB，以及下次启动前的轮转结果。
- 默认与完整诊断输出中均不存在 API Token、私网密钥或设备私钥内容。

在 `.4` 源码或上述真机证据缺失时，报告应使用“待验收”而非“已证明”。
