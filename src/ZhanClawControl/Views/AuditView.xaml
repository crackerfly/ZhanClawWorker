<UserControl x:Class="ZhanClawControl.Views.AuditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="220" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="任务审计" Style="{StaticResource PageTitle}" Margin="0,0,0,4" />
            <TextBlock Style="{StaticResource CaptionText}"
                       Text="本机收到并执行过的远端任务记录。远端 PowerShell 拥有本机 Agent 账户的完整权限，这里可以核对到底发生了什么。" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="8,0,0,0">
                <Button Content="刷新" Command="{Binding RefreshCommand}" Margin="0,0,8,0" />
                <Button Content="复制诊断信息" Style="{StaticResource AccentButton}"
                        Command="{Binding CopyDiagnosticsCommand}" MinWidth="118" Margin="0,0,8,0" />
                <Button Content="保存诊断" Command="{Binding SaveDiagnosticsCommand}" Margin="0,0,8,0" />
                <Button Content="导出记录" Command="{Binding ExportJournalCommand}" Margin="0,0,8,0" />
                <Button Content="打开数据目录" Command="{Binding OpenDataFolderCommand}" MinWidth="110" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,0,0,12">
            <DockPanel>
                <TextBlock DockPanel.Dock="Bottom" Text="{Binding JournalSummary}"
                           Style="{StaticResource CaptionText}" Margin="0,10,0,0" />
                <DataGrid ItemsSource="{Binding Records}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="时间" Binding="{Binding TimestampText}" Width="130" />
                        <DataGridTextColumn Header="来源设备" Binding="{Binding ShortSourcePeer}" Width="160" />
                        <DataGridTextColumn Header="动作" Binding="{Binding Action}" Width="170" />
                        <DataGridTextColumn Header="阶段" Binding="{Binding State}" Width="110" />
                        <DataGridTextColumn Header="结果" Binding="{Binding Status}" Width="100" />
                        <DataGridTextColumn Header="耗时" Binding="{Binding DurationText}" Width="80" />
                        <DataGridTextColumn Header="Command ID" Binding="{Binding ShortCommandId}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <Grid Grid.Row="3" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="运行日志" Style="{StaticResource SectionTitle}" Margin="0,0,10,0" />
                <TextBlock Text="每行前缀为本机时间；[agent] 为 Agent 自身输出，[host] 为后台宿主"
                           Style="{StaticResource CaptionText}" VerticalAlignment="Center" />
            </StackPanel>
            <Button Content="清空日志" HorizontalAlignment="Right" Command="{Binding ClearLogCommand}" />
        </Grid>

        <Border Grid.Row="4" Style="{StaticResource Card}" Margin="0" Padding="0">
            <TextBox Text="{Binding LogText, Mode=OneWay}" IsReadOnly="True"
                     BorderThickness="0" Background="Transparent"
                     FontFamily="Consolas, Cascadia Mono, Courier New" FontSize="12"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     TextWrapping="NoWrap" Padding="14,12" VerticalContentAlignment="Top" />
        </Border>
    </Grid>
</UserControl>
